<nav class="navbar navbar-expand-md bg-white shadow-sm border-bottom sticky-top py-3">
  <div class="container-fluid">

    <!-- Brand -->
    <a class="navbar-brand fw-bold text-danger d-flex align-items-center gap-2" href="/">
      <i class="fa-brands fa-airbnb fs-3"></i>
      StayEase
    </a>

    <!-- Mobile toggle -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar content -->
    <div class="collapse navbar-collapse" id="navbarNav">
      
      <!-- Center Search (only on md+) -->
      <form class="mx-auto d-none d-md-flex align-items-center rounded-pill border px-3 py-1 gap-2 shadow-sm" style="max-width: 450px; background: #fff;">
        <input type="text" class="form-control border-0 shadow-none" placeholder="Search stays..." style="font-size: 0.9rem;">
        <button type="submit" class="btn btn-danger rounded-circle d-flex align-items-center justify-content-center" style="width: 36px; height: 36px;">
          <i class="fa-solid fa-magnifying-glass text-white"></i>
        </button>
      </form>

      <!-- Links -->
      <div class="navbar-nav ms-auto align-items-center gap-2">

        <a class="nav-link fw-semibold px-2 d-flex align-items-center gap-1" href="/">
          <i class="fa-solid fa-compass text-danger"></i> Discover
        </a>

        <a class="nav-link fw-semibold px-2 d-flex align-items-center gap-1" href="/listings">
          <i class="fa-solid fa-globe text-danger"></i> Explore
        </a>

        <a class="nav-link fw-semibold px-2 d-flex align-items-center gap-1" href="/listings/new">
          <i class="fa-solid fa-plus text-danger"></i> List Your Stay
        </a>

        <% if(typeof currUser === 'undefined' || !currUser){ %>
          <a class="btn btn-outline-danger rounded-pill px-3 py-1 fw-semibold d-flex align-items-center gap-1" href="/signup">
            <i class="fa-solid fa-user-plus"></i> Sign Up
          </a>
          <a class="btn btn-danger text-white rounded-pill px-3 py-1 fw-semibold d-flex align-items-center gap-1" href="/login">
            <i class="fa-solid fa-right-to-bracket"></i> Login
          </a>
        <% } %>

        <% if(typeof currUser !== 'undefined' && currUser){ %>
          <a class="btn btn-outline-secondary rounded-pill px-3 py-1 fw-semibold d-flex align-items-center gap-1" href="/logout">
            <i class="fa-solid fa-right-from-bracket"></i> Logout
          </a>
        <% } %>

      </div>
    </div>
  </div>
</nav>
